export interface Customer {
  id: string
  name: string
  phoneNumber: string
  address: string
  isActive: boolean
  createdAt: string
  updatedAt: string
}

export interface CustomerResponse {
  id: string
  name: string
  phoneNumber: string
  address: string
  isActive: boolean
  createdAt: string
  updatedAt: string
}